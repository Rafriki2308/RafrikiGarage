<form method="post" action="{{ path('app_customer_softCar', {'id': car.id}) }}" onsubmit="return confirm('Esta Seguro que desea eliminar el elemento?');">
    <br/>
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car.id) }}">
    <button class="btn">Borrar</button>
</form>
