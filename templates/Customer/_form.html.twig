{{ form_start(form) }}
    {{ form_widget(form) }}
<button type="{{ button_type|default('submit') }}"
        class="{{ button_class|default('btn btn-primary') }}"
        data-bs-toggle="modal"
        data-bs-target="{{ target|default('#staticBackdrop') }}">
    {{ button_label|default('Guardar')}}</button>

<!--Modal-->
<div class="modal fade" id="staticBackdropEdit" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="staticBackdropLabel">Modificar Cliente</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                Esta a punto de modificar los datos del cliente.
                ¿Esta seguro que desea hacerlo?
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button>
                <button type="submit" class="btn btn-primary">Entendido</button>
            </div>
        </div>
    </div>
</div>
{{ form_end(form) }}
